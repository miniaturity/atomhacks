<header>
    [ISS_TRACKER]
</header>

<style lang="scss">
    header {
        --width: 150px;
        --pad: 5px;
        color: var(--primary);
        font-family: "Geist";
        font-size: 1.8rem;
        font-weight: bold;
        position: absolute;
        top: var(--pad);
        width: var(--width);
        left: calc(50% - calc(var(--width) / 2));
    }
</style>